<section class="content-header">
  <div class="container-fluid">
    @if(title)
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1>
            @if(icon)
              <i class="{{ icon }}"></i>
            @endif
            {{ title }}
          </h1>
        </div>
        <div class="col-sm-6">
          {{{ $slots.main() }}}
        </div>
      </div>
    @endif
  </div>
</section>